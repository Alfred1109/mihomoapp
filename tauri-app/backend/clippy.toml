# Clippy configuration for better Rust code quality

# Deny common mistakes
avoid-breaking-exported-api = true
msrv = "1.70"

# Pedantic lints (optional, can be enabled gradually)
# These are more strict and may require more refactoring
